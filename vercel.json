{
  "buildCommand": "echo 'Static site - no build required'",
  "outputDirectory": ".",
  "routes": [
    { "src": "^/api/(.*)", "dest": "/api/$1" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}